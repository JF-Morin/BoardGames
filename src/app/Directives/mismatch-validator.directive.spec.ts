import { MismatchValidatorDirective } from './mismatch-validator.directive';

describe('MismatchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MismatchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
