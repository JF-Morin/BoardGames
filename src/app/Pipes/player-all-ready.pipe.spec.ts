import { PlayerAllReadyPipe } from './player-all-ready.pipe';

describe('PlayerAllReadyPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerAllReadyPipe();
    expect(pipe).toBeTruthy();
  });
});
